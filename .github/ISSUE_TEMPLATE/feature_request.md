---
name: Feature request
about: Suggest a new wash rule, tag, attribute, font mapping, or CLI workflow
title: "[feat] "
labels: ["enhancement"]
assignees: []
---

## The problem
<!-- What are you trying to do that `gzhseam wash` currently blocks, mangles, or drops? -->

## Proposed change
<!-- A concrete description of the desired behavior — e.g. "keep the <details> tag", "map font-family 'JetBrains Mono' to the mono bucket", "add a `gzhseam wash --strict` flag". -->

## Repro / context

**gzhseam version** (run `gzhseam --version`):
```
gzhseam 0.2.0
```

**Environment**:
- OS:
- Python: <!-- `python --version` -->

**Input HTML snippet** (if relevant):
```html
<!-- paste an example -->
```

**Exact command you wish behaved differently**:
```
gzhseam wash deck.html -o out.html
```

## Alternatives considered
<!-- Any workarounds you tried (manual edits, a different font, skipping the stage). -->

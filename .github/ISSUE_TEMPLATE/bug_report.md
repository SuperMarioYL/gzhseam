---
name: Bug report
about: Report a crash, traceback, or wrong output from `gzhseam wash`
title: "[bug] "
labels: ["bug"]
assignees: []
---

## What happened?
<!-- A clear description of what went wrong — e.g. `gzhseam wash` aborted with a traceback, or the output HTML was missing tags / mangled styles. -->

## What did you expect?
<!-- What you expected the washed HTML to look like / contain. -->

## Repro

**gzhseam version** (run `gzhseam --version`):
```
gzhseam 0.2.0
```

**Environment**:
- OS: <!-- e.g. macOS 14 / Ubuntu 22.04 / Windows 11 -->
- Python: <!-- `python --version`, must be 3.12+ -->

**Input HTML snippet** (the smallest fragment that reproduces the issue):
```html
<!-- paste the smallest HTML that triggers the bug here -->
```

**Exact command**:
```
gzhseam wash deck.html -o out.html
```

<!-- add `--cdn` here only if your repro needs the m2 CDN stage; m2 is a stub in v0.2.0 -->

**Observed output / traceback**:
```
<!-- paste the traceback, or the wrong output HTML, here -->
```

## Anything else?
<!-- Screenshots, the full deck (if small), or context about the coding agent (Claude Code / Cursor / a Skill) that produced the HTML. -->
